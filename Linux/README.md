# Linux Setup
A few prerequisits are required to run FastEPD on the Raspberry Pi:<br>
Run setup.sh to install the necessary libraries.<br>
Enable I2C using raspi-config. Select "Interface Options" and enable I2C:<br>
sudo raspi-config<br>

